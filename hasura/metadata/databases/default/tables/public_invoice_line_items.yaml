table:
  name: invoice_line_items
  schema: public
object_relationships:
  - name: invoice
    using:
      foreign_key_constraint_on: invoice_id
insert_permissions:
  - role: finance_user
    permission:
      check: {}
      set:
        company_id: X-Hasura-Company-Id
      columns:
        - invoice_id
        - description
        - note
        - quantity
        - unit_price
        - line_amount
        - unit
        - hsn_sac
        - gst_rate
  - role: company_admin
    permission:
      check: {}
      set:
        company_id: X-Hasura-Company-Id
      columns:
        - invoice_id
        - description
        - note
        - quantity
        - unit_price
        - line_amount
        - unit
        - hsn_sac
        - gst_rate
select_permissions:
  - role: finance_user
    permission:
      columns: "*"
      filter: { company_id: { _eq: X-Hasura-Company-Id } }
  - role: approver
    permission:
      columns: "*"
      filter: { company_id: { _eq: X-Hasura-Company-Id } }
  - role: company_admin
    permission:
      columns: "*"
      filter: { company_id: { _eq: X-Hasura-Company-Id } }
  - role: genai_readonly
    # invoiceSummary's lineItems relationship (services/genai-service's
    # _INVOICE_SUMMARY_CTX query) needs this to traverse invoices.lineItems.
    permission:
      columns:
        - description
        - quantity
        - unit_price
      filter: { company_id: { _eq: X-Hasura-Company-Id } }
  - role: genai_writer
    # Needed for the `lineItems` relationship under invoices in
    # _INVOICE_FULL — cross-tenant, same as genai_writer's invoices grant.
    permission:
      columns:
        - description
        - quantity
        - unit_price
      filter: {}
