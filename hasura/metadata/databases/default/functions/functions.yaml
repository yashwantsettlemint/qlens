- function:
    name: match_invoice_embeddings
    schema: public
  configuration:
    exposed_as: query
  permissions:
    - role: genai_readonly
- function:
    name: upsert_invoice_embedding
    schema: public
  configuration:
    exposed_as: mutation
  # admin-only (built-in) — genai-service /embed calls this with the admin secret
